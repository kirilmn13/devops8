name: Hello World Innotech

on:
  workflow_dispatch:
  issues:
    types: [opened, edited]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the User
        run: echo "Hello World!"